[project]
name = "ai-doc-extractor"
version = "0.1.0"
description = "Deterministic invoice/receipt extractor with arithmetic validation and offline gold-set evaluation."
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
]

[tool.ruff]
line-length = 100
